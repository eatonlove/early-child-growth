# 由 `npm run local:prepare` 自动生成 deploy/local-lite/.env。
# 以下值仅用于本机验证，禁止复制到生产环境。
POSTGRES_PASSWORD=tongji_local_postgres
LOCAL_JWT_SECRET=replace-with-at-least-32-local-only-characters
LOCAL_ANON_KEY=generated-by-local-prepare
LOCAL_SERVICE_ROLE_KEY=generated-by-local-prepare
